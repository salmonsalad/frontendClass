import React from 'react';
import ReactDOM from 'react-dom/client';
import KurlyComponent from './components/KurlyComponent';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <KurlyComponent />
);
