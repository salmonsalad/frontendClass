import React from 'react';
import ReactDOM from 'react-dom/client';
import PofoComponent from './components/PofoComponent';



const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PofoComponent />
  </React.StrictMode>
);

